name: peg
description: Command line tool for generating PEG parsers with support for event-based parsing.
version: 1.0.0
homepage: https://github.com/mezoni/peg
topics:
  - parser
  - peg
environment:
  sdk: '>=3.0.0 <4.0.0'
dependencies:
  args: ^2.4.2
  dart_style: ^2.2.4
  path: ^1.8.3
  strings: ^2.0.3
dev_dependencies:
  lints: ^2.0.0
  parser_builder_lite: ^0.7.3
  test: ^1.24.4