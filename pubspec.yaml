name: peg
description: A command line tool for generating (streaming, chunk, file) top-down parsers from a parsing expression grammars (PEG).
version: 2.0.2
homepage: https://github.com/mezoni/peg
topics:
  - parser
  - parser-generator
  - peg
  - top-down-parser
environment:
  sdk: '>=3.0.0 <4.0.0'
dependencies:
  args: ^2.4.2
  path: ^1.8.3
  strings: ^2.0.3
dev_dependencies:
  lints: ^2.0.0
  test: ^1.24.7
executables:
  peg: