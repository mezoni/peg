name: peg
description: A command line tool for generating top-down parsers from a parsing expression grammars (PEG).
version: 1.0.29
homepage: https://github.com/mezoni/peg
topics:
  - parser
  - parser-generator
  - peg
  - top-down-parser
environment:
  sdk: '>=3.0.0 <4.0.0'
dependencies:
  args: ^2.4.2
  dart_style: ^2.2.4
  path: ^1.8.3
  strings: ^2.0.3
dev_dependencies:
  lints: ^2.0.0
  test: ^1.24.4
executables:
  peg: